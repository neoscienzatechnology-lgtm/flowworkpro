# PostgreSQL — copie a connection string do Neon (neon.tech) ou Supabase
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Chave secreta JWT (troque por uma string aleatória longa)
JWT_SECRET="troque-por-uma-chave-secreta-segura-aqui"

# Porta local (opcional)
PORT=3001
